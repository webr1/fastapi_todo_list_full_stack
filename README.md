# Todo App — FastAPI Backend + HTML/JS Frontend

Это простое ToDo-приложение. Бэкенд сделан на FastAPI, фронтенд — на чистом HTML/CSS/JS.

Фронтенд отправляет запросы в backend по HTTP и отображает задачи.

---

## 🚀 Как установить и запустить приложение

Ниже — полный процесс запуска backend и frontend.

---

# 1) УСТАНОВКА BACKEND

## 1. Перейдите в папку backend:

```bash
cd backend
2. Создайте виртуальное окружение:

python -m venv venv

3. Активируйте окружение:
Windows:
venv\Scripts\activate

Linux/Mac:
source venv/bin/activate

4. Установите зависимости:
Если есть requirements.txt:
pip install -r requirements.txt
pip install fastapi uvicorn sqlalchemy asyncpg python-multipart

2) ЗАПУСК BACKEND

Выполните команду:
uvicorn main:app --reload

Backend будет доступен по адресу:
http://127.0.0.1:8000/docs

Документация:
И API для задач должны быть такими:

| Метод | Endpoint               | Описание            |
|-------|-------------------------|----------------------|
| GET   | /tasks/tasks_all        | Получить все задачи |
| POST  | /tasks/add_tasks        | Создать задачу      |
| PUT   | /tasks/update/{id}      | Обновить задачу     |
| DELETE| /tasks/delete/{id}      | Удалить задачу      |

```js
const API = "http://127.0.0.1:8000/tasks";

Backend должен возвращать JSON с полями:

    {
      "id": 1,
      "title": "string",
      "description": "string",
      "is_completed": false
    }

4) ЗАПУСК FRONTEND

Фронтенд НЕ требует NPM или сервера.

Перейдите в папку:

#frontend/

Найдите файл:
index.html

Откройте его двойным кликом в браузере (Chrome, Firefox).
Фронтенд сразу заработает, если backend запущен.

















